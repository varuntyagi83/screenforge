'use client'

import { RecordingEngine } from '@/components/recorder/RecordingEngine'

export default function RecordPage() {
  return (
    <main className="min-h-screen bg-gray-50">
      <RecordingEngine />
    </main>
  )
}
