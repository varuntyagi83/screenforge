{
  "name": "screenforge-extension",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "dev": "vite build --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.300",
    "typescript": "^5",
    "vite": "^6"
  }
}
